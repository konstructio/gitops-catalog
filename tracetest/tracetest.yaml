---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tracetest-components
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: '10'
spec:
  project: <PROJECT>
  source:
    repoURL: <GITOPS_REPO_URL>
    path: <REGISTRY_PATH>/components/tracetest
    targetRevision: HEAD
  destination:
    name: <CLUSTER_DESTINATION>
    namespace: tracetest
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
