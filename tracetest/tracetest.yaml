---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: tracetest-<ENVIRONMENT>-components
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: '10'
spec:
  project: default
  source:
    repoURL: <GITOPS_REPO_URL>
    path: <REGISTRY_PATH>/components/tracetest
    targetRevision: HEAD
  destination:
    name: in-cluster
    namespace: tracetest
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
